[package]
name = "blockmod"
version = "0.1.0"
edition = "2021"

[features]
inspector = ["dep:bevy-inspector-egui"]

[dependencies]
bevy = "0.7.0"
bevy-inspector-egui = { version = "0.10.0", optional = true }

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3